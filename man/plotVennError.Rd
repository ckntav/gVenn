% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotVennError.R
\name{plotVennError}
\alias{plotVennError}
\title{Plot where a Venn diagram misrepresents the data}
\usage{
plotVennError(venn, ...)
}
\arguments{
\item{venn}{A plot returned by \code{\link{plotVenn}}, carrying the
\code{"euler_fit"} attribute that function attaches. Note that this is the
\code{plotVenn()} output, not the \code{GenomicOverlapResult} or
\code{SetOverlapResult} that \code{plotVenn()} itself takes.}

\item{...}{Additional arguments passed to \code{\link[eulerr]{error_plot}}.}
}
\value{
The plot returned by \code{\link[eulerr]{error_plot}}, a \code{gTree}
that can be drawn, arranged with other grobs, or written out with
\code{\link{saveViz}}.
}
\description{
Redraws the Venn diagram produced by \code{\link{plotVenn}}, shaded by the
signed error of each region, so that the regions the diagram gets wrong can
be read off the picture rather than inferred from a single summary
statistic. This is a thin wrapper around \code{\link[eulerr]{error_plot}}.
}
\details{
Where \code{\link{plotVenn}}'s \code{diagError} answers "how far off is the worst
region", this answers "which regions, and in which direction". A region
drawn too large is shaded towards one end of the scale and one drawn too
small towards the other, with its \code{regionError} printed inside it.

It is worth reaching for whenever \code{diagError} is above the 1e-6 threshold
\code{\link{plotVenn}} reports against, or whenever
\code{attr(venn, "fit_diagnostics")$undrawnRegions} is not empty, since a region
can be erased while \code{diagError} itself stays small.

It takes the plot returned by \code{\link{plotVenn}} rather than the overlap
object, and reuses the fit stored on it instead of computing a new one.
\code{eulerr::euler()} draws its optimiser seed from R's random number generator,
so refitting the same data can settle on a different placement: repeated
fits of one three-set input have been observed to differ in both ellipse
centres and radii. An error plot built from such a refit would describe a
diagram the user never saw, and its shaded regions would not line up with
the ones in front of them, which is the only thing that makes the plot worth
reading. Reusing the stored fit also avoids paying for the optimisation
twice.
}
\examples{
data(gene_list)
res_sets <- computeOverlaps(gene_list)

# Where does the diagram misrepresent the counts?
venn <- plotVenn(res_sets)
plotVennError(venn)
}
\seealso{
\code{\link{plotVenn}} for the diagram itself and its fit
diagnostics, \code{\link{plotUpSet}} for a representation that does not
approximate any count by an area.
}
